## Run Locally

This repository contains the Kurama web app. To run locally:

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Create a `.env.local` from `.env.example` and fill any required API keys
3. Run the app:
   `npm run dev`
